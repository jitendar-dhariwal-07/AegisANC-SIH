# Dashboard setup

The primary dashboard uses Vite, React, and TypeScript. Run `cd frontend`, `npm install`, and `npm run dev`. Web Serial should only be requested from a user-initiated Connect action in the hardware integration. The optional fallback runs with `pip install -r dashboard/requirements.txt` followed by `streamlit run dashboard/app.py`.
