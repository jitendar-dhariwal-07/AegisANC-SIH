# Calibration

Measure a quiet baseline, then reference input, then begin at low output gain. Reset the adaptive filter, test speech, and test alarm passthrough at low speaker volume. Save relative RMS comparisons only. Uncalibrated prototype values must not be interpreted as certified dB SPL.
